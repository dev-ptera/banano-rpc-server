{
    "name": "sample-app",
    "version": "0.0.0",
    "description": "Demoing use of @dev-ptera/nano-rpc-proxy",
    "license": "MIT",
    "scripts": {
        "build": "tsc --outDir dist/dev",
        "build:prod": "tsc --outDir dist/prod",
        "start": "yarn build && cd dist/dev && node app.js",
        "start:prod": "yarn build:prod && cd dist/prod && node app.js production"
    },
    "private": true,
    "dependencies": {
        "@dev-ptera/nano-rpc-proxy": "latest"
    },
    "devDependencies": {
        "@types/node": "^14.14.34",
        "@types/cors": "^2.8.10",
        "@types/express": "^4.17.11",
        "tslib": "^2.1.0",
        "typescript": "~4.0.2"
    }
}
